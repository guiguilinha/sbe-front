services:
  # Frontend
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    networks:
      - maturidade-network

networks:
  maturidade-network:
    external: true
    name: maturidade_digital_network
