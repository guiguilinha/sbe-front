/**
 * Índice de hooks do frontend
 * Centraliza todas as exportações de hooks
 */

// Hooks da Home Page
export { useHomeData } from './useHomeData';

// Hooks temporários (serão substituídos quando implementarmos as páginas)
export { useQuizData } from './useQuizData';
export { useResultsPage } from './useResultsPage';

// Hooks utilitários
export { useIsMobile } from './use-mobile';

// Hooks de autenticação e acesso
export { useQuizAccess } from './useQuizAccess'; 